/**
 * LAB — §18. Experimental work that is not client work.
 *
 * Empty on purpose. Nothing has been made for it yet, and §39/§41 forbid
 * dressing the section with invented or borrowed material, so while this array
 * is empty the LAB section renders its typography and an explicit empty state.
 *
 * Add entries shaped like:
 *   { slug, title, note, media: { kind: 'video'|'image', name, w, h, alt } }
 * where `name` is a file in /public/media/{loop,poster,image}. Anything
 * generated rather than filmed must set `generated: true` — the section labels
 * it so AI work is never mistaken for commissioned work (§20).
 */

/** @type {Array<Object>} */
export const lab = [];
